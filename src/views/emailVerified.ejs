<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Email Verification | Greenhouse</title>
    <style>
      :root {
        --clr-primary: #00b2a8;
        --clr-secondary: #69e1dd;
        --clr-text: #696b75;
        --clr-tertiary: #f69da3;
      }

      @import url("https://fonts.googleapis.com/css2?family=Open+Sans:wght@300;400;500&display=swap");

      body {
        font-family: "Open Sans", sans-serif;
        background-color: #fff;
        color: var(--clr-text);
        line-height: 1.5;
      }

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      img {
        object-fit: contain;
        width: 100%;
        height: 100%;
      }

      main {
        padding: 8rem 4rem;
        background-color: #cbe9e8;
        height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
      }

      .verif-email,
      .verified {
        position: relative;
        background-color: #fff;
        margin: 0 auto;
        display: flex;
        justify-content: space-between;
        width: 85%;
        padding: 3rem;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
      }

      .verified {
        width: 80%;
      }

      .company-label {
        position: absolute;
        top: -50px;
        left: 50%;
        transform: translate(-50%, -50%);
        color: var(--clr-primary);
        font-family: inherit;
        font-weight: bold;
        letter-spacing: 1px;
        font-size: 22px;
        opacity: 0.3;
      }

      .verif-img-box {
        flex: 0 0 40%;
        margin-right: 1rem;
      }

      .verif-info-box {
        margin-left: 1rem;
        flex: 0 0 50%;
      }

      .verif-welcome {
        text-align: right;
        margin-bottom: 5rem;
        font-size: 35px;
        color: var(--clr-primary);
      }
      .verif-underline {
        display: none;
      }

      .verif-btn {
        border: none;
        outline: none;
        cursor: pointer;
        color: #fff;
        background-color: var(--clr-primary);
        font-size: 16px;
        font-weight: bold;
        padding: 1rem 1.2rem;
        letter-spacing: 1px;
        font-family: inherit;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        border-radius: 2px;
        transition: all 0.2s;
        width: 100%;
      }

      .verified .verif-btn {
        width: 90%;
      }
      .verif-btn:hover {
        background-color: rgba(0, 178, 169, 0.511);
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
      }

      .verif-link {
        width: 100%;
        display: inline-block;
        margin: 2rem 0;
      }

      @media screen and (max-width: 1023px) {
        .verif-email {
          width: 100%;
        }
        .verif-img-box {
          flex: 0 0 45%;
        }
        .verif-welcome {
          font-size: 33px;
        }
      }
      @media screen and (max-width: 767px) {
        main {
          height: auto;
        }
        .verif-email {
          width: 100%;
          flex-direction: column;
        }
        .verified {
          width: 75%;
        }
        .verif-img-box {
          margin: 0;
        }
        .verif-info-box {
          text-align: center;
        }
        .verif-welcome {
          text-align: center;
          margin: 2rem 0;
        }
        .verif-underline {
          display: block;
          width: 100%;
          height: 2px;
          background-color: #f1f1f1;
          margin: 1rem 0;
        }

        .verif-description {
          margin: 0 auto;
          font-size: 13px;
          width: 80%;
        }
      }

      @media screen and (max-width: 479px) {
        main {
          padding: 8rem 2rem;
        }
        .verif-email {
          padding: 3rem 2rem;
        }

        .verified {
          padding: 2rem;
          width: 100%;
        }

        .verified .verif-welcome {
          font-size: 30px;
          margin: 1rem 0;
        }

        .verified .verif-btn {
          width: 100%;
        }
      }
    </style>
  </head>
  <body>
    <main>
      <section class="verified verif-email">
        <p class="company-label">TwentyFifty7</p>
        <div class="verif-img-box">
          <img src="./img/verified.png" alt="" class="verif-img" />
        </div>
        <div class="verif-info-box">
          <h1 class="verif-welcome">Verified!</h1>
          <div class="verif-underline"></div>
          <p class="verif-description">
            You have successfully verified the account.
          </p>
        </div>
      </section>
    </main>
  </body>
</html>
